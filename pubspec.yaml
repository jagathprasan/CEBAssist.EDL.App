name: electricity_board_erp
description: "CEBAssist — Electricity Board ERP mobile application."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^17.5.0
  fl_chart: ^1.2.0
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^9.2.4
  http: ^1.5.0
  google_fonts: ^8.2.1
  intl: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
